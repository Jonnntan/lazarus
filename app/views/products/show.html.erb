<html>

<body>
  <div style="text-align: center">
    <%= link_to "Back to #{@product.brand.name} Products", brand_products_path(@product.brand.id), :class => "link" %>
    <%= link_to 'Home', root_path, :class => 'link' %>
  </div>
  <ul>
    <table>
      <tr>
        <th>ID</th>
        <th>Brand</th>
        <th>Name</th>
        <th>Description</th>
        <th>Price</th>
        <th>Actions</th>
      </tr>
      <tr>
        <td><%= @product.id %></td>
        <td><%= @product.brand.name %></td>
        <td><%= @product.title %></td>
        <td><%= @product.description %></td>
        <td><%= @product.price %></td>
        <td><a href='<%= edit_product_path(@product) %> ' class="link">Update</a>
    <%= link_to "Delete", product_path(@product), method: :delete, data: {confirm: 'Delete?'}, :class => "link" %></td>
      </tr>
    </table>

  </ul>
</body>

</html>