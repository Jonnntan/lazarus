<html>

<body>
  <div style="text-align: center">
    <%= link_to "Show all #{@product.title} Variant(s)", product_variants_path, :class => 'link' %>
    <%= link_to 'Home', root_path, :class => 'link' %>
  </div>
  <ul>
    <table>
      <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Price</th>
      </tr>
      <tr>
        <td><%= @variant.id %></td>
        <td><%= @variant.name %></td>
        <td><%= @variant.price %></td>
      </tr>
    </table>
  </ul>
</body>

</html>